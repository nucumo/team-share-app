export interface User {
  id: string;
  name: string;
  avatarImage: string;
  coverImage: string;
  birthDate: Date;
  location: string;
}

// it's test project, so I don't divide user data and other member data
